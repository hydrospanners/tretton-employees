import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    h1: {
      fontSize: "1.5rem",
      fontWeight: 200,
    },
    h2: {
      fontSize: "1.2rem",
      fontWeight: 500,
    },
    h3: {
      fontSize: "1rem",
      fontWeight: 700,
    },
    body1: {
      fontWeight: 200,
    },
  },
});
